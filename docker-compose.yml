name: momo-store

services:
  backend:
    image: ${DOCKER_USER:-fetbubun}/docker-project-backend:latest
    build:
      context: ./backend
    expose:
      - "8081"
    networks:
      - backend_net
    volumes:
      - backend_data:/app/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:rw,size=16m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8081/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M
    profiles: ["dev", "prod"]

  frontend:
    image: ${DOCKER_USER:-fetbubun}/docker-project-frontend:latest
    build:
      context: ./frontend
      args:
        VUE_APP_API_URL: /api
    expose:
      - "8080"
    networks:
      - frontend_net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:rw,size=16m
      - /var/cache/nginx:rw,size=16m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
    profiles: ["dev", "prod"]

  nginx:
    image: ${DOCKER_USER:-fetbubun}/docker-project-nginx:latest
    build:
      context: ./nginx
    ports:
      - "${NGINX_HOST_PORT:-80}:8080"
    networks:
      - frontend_net
      - backend_net
    volumes:
      - nginx_logs:/var/log/nginx
    secrets:
      - source: docker_password
        target: docker_password
        mode: 0400
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:rw,size=16m
      - /var/cache/nginx:rw,size=16m
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 64M
    profiles: ["dev", "prod"]

volumes:
  backend_data:
  nginx_logs:

networks:
  frontend_net:
    driver: bridge
  backend_net:
    driver: bridge

secrets:
  docker_password:
    file: ./secrets/docker_password.txt
